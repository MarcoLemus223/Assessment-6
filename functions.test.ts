const {shuffleArray} = require('./utils')

describe('shuffleArray should', () => {
    // CODE HERE
    test('returning a array', async () => {
        return 3
    })
})

describe('shuffleArray should', () => {
    test('return name', async () => {
        return 'Marco'
    })
})

describe('shuffArray should', () => {
    test('add 1 and 3', async () => {
        return 4
    })
})